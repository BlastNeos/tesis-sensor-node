services:
  broker:
    image: eclipse-mosquitto:2.0-alpine
    container_name: tesis-mosquitto
    restart: unless-stopped
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
